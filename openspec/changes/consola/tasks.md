# Tasks

## 1. Vista de consola

- [ ] 1.1 Crear `vista/VistaConsola.java` con constructor `(Scanner, PrintStream)` y el bucle de una partida: mostrar partida, pedir jugada, validar formato y rango, jugar y avisar de casilla ocupada (verificar: tests de jugada válida, entrada no numérica, fuera de rango y casilla ocupada)
- [ ] 1.2 Mostrar el resultado (Gana X, Gana O, Empate) y preguntar por otra partida, terminando con "n" o fin de entrada (verificar: tests de victoria, empate, otra partida, salir y fin de entrada)
- [ ] 1.3 Actualizar `Main` para lanzar la vista con `System.in` y `System.out` (verificar: prueba manual de una partida completa ejecutando el jar)
- [ ] 1.4 Javadoc de la clase y sus métodos públicos, con el motivo de la comparación de `toString()` (verificar: revisión del código)
